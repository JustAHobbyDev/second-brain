interface:
  display_name: "PM Recurring Cycle"
  short_description: "Run deterministic PM consult cycles"
  default_prompt: "Run one PM recurring cycle: inspect state, propose next decision, execute selected branch, and update scene artifacts via sb_agent_run_cycle_v0.sh."

# Audit Reports v0

This directory stores track-only multi-agent audit artifacts for the authority model v0.

Conventions:
- One report per audit run.
- File format may be JSON or Markdown.
- Reports are append-only evidence; no auto-mutation hooks.
